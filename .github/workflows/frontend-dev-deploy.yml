name: 'Frontend Dev Deploy (Test)'
on:
  push:
    tags:
      - 'podonos-apple-frontend-dev-*'
jobs:
  deploy-test:
    runs-on: 'ubuntu-latest'
    steps:
      - name: 'Print tag info'
        run: |
          echo "Triggered by tag: ${{ github.ref_name }}"
          echo "Commit SHA: ${{ github.sha }}"
          echo "Workflow is working correctly!"
